#!/usr/bin/env ruby

require 'logn'

events = []

ARGF.set_encoding 'utf-8'
puts start = Time.now
ARGF.each_line do |line|
  events << Logn::Event.new(line)
end
puts Time.now - start

require 'pry'
binding.pry
